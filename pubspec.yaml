name: fast_cache_network_image
description: A flutter package to cache network image easily.You can add beautiful loaders, percentage indicators and error builder.
version: 1.0.5
homepage: https://github.com/deepak07082/fast_cached_network_image

topics:
  - cache
  - image
  - photo
  - download
  - network


environment:
  sdk: '>=2.18.0 <4.0.0'
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.9.0
  hive_ce_flutter: ^2.3.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
